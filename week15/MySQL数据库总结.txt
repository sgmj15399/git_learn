MySQL是关系型数据库，当引擎为Innodb时，支持事务操作，索引查询；
MySQL数据库的默认隔离级别时可重复读，是通过MVCC多版本并发控制来实现的；
当SQL运行很慢时，我们可以通过慢查询日志和监控发现需要优化的SQL，针对慢SQL，我们在优化SQL需要注意以下问题：
1.大批量写入SQL解析慢；
2.数据更新注意间隙锁的问题；
3.索引失效的问题；如：模糊查询like%在前，NULL，not，not in，函数,使用 or等操作；
4.连接查询驱动表的选择问题；
