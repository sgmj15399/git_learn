缓存是为了加速数据处理，让业务更快访问而临时存放冗余的数据；
缓存的本质是解决系统各级处理速度不匹配，导致利用空间换时间；
缓存分为本地缓存和远程缓存，本地缓存有Guava Cache，远程缓存有Redis/MemCache等；
使用缓存需要考虑的问题有缓存数据容量，过期策略，缓存穿透，缓存击穿，缓存雪崩，缓存单点失效等问题；
在分布式环境下一般我们会用redis作为缓存组件；因为他可以提供过期策略，持久化，集群等多种功能；

















